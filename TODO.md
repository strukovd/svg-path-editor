- Реализовать добавление примитивов: `rect`, `image`, `circle`, `ellipse`, `line`
- Обкатать на них контейнер (фальшивый bounding-box), с (наследуемыми) композаблами для selectable (сам выделяемый комнтейнер), resize, moving, rotate
  - Для Selectable предусмотеть множественное выделение
- Далее нужно либо парсить файл svg либо реализация path






# То что планирую реализовать
- [ ] Сохранение кастомных параметров в localStorage
	- Возможность кастомизировать параметры редактора (цвета, размеры, stepScale, и прочее)
	- Инициализировать кастомные параметры из localStorage

- Рисование
	- Выделять и копировать фигуры
	- Перемещать группу точек
	- CTRL+D дублировать выделенное
	- Выравнивать линию (с зажатым shift)
	- Пропорционально растягивать рисунок (несколько выделенных точек)
	- Нужна линейка для сравнения расстояний.
	- Линейка относительной которой можно было бы ровнятся
	- При выборе точки, в контекстное меню добавить (добавить гориз/вертик линейку здесь)
	- Зеркально разворачивать вставленную картинку и сам path
	- Добавить возможность ограничения размеров холста (чтобы подгонять например 250x250)
	- Показывать количество промежутка между двумя выбранными точками (как в uxpin, появляется розовая полоса между друмя выделенными объектами), и сделать это для линейки, что бы выстроить ровную сетку
	- Добавить возможность выделения точек (при зажатом ctrl+mousedown), затем групповые возможности над выделенными точками.

- Несколько path секций (группы)
- CTRL+D дублировать выделенное
- Подсветка синтаксиса path
- Нужна линейка для сравнения расстояний.
- Возможность вставки картинки из буффера
- ? Экспорт отдельных частей картинки
- Зеркально разворачивать вставленную картинку и сам path
- Добавить возможность ограничения размеров холста (чтобы подгонять )
- При наведении на точку показывать подсказку с координатами точки, и длину линии
- Добавить линейку относительной которой можно было бы ровнятся
- Показывать количество промежутка между двумя выбранными точками (как в uxpin, появляется розовая полоса между друмя выделенными объектами), и сделать это для линейки, что бы выстроить ровную сетку
- При выборе точки, в контекстное меню добавить (добавить гориз/вертик линейку здесь)
- Добавить возможность выделения точек (при зажатом ctrl+mousedown), затем групповые возможности над выделенными точками.
- Работа с группой точек:
	Предположительно: Так как я планирую использовать оптимальную структуру данных для хранения
	и подгрузки только видимых точек (квадричное дерево или что-то в это роде),
	то выделенные точки хранить в отдельном едином массиве Array<Points> (не зависимо от их расположения),
	над группой точек дать возможность манипуляии (копирование, вращение, перемещение, масштабирование, и т.д.)
	- Автоматическое скругление углов (при выборе точке на угла, по нажатию на кнопку эта точка должна перврарится в скругленную Curve-bezie (C), и точку.
	- Нужно перетаскивание и трансформирование (крутить, масштаб) любой линии.
- Так же нужен режим относительности, то есть, выбрать некую точку, и пока она выбрана перетаскивая другую точку (или группу точек) показывать растояние от той выбранной точки.
- Нету возможности вставить картинку из буфера обмена, а только из файловой системы.

TODO:
- на ПКМ показывать круглое меню (как в блендер), где будет возможность выбрать один из типов добавляемой фигуры (линия, безье, ..).
- После добавления первой точки, показывать курсором (или где-то снизу) следующую фигуру, простой прокруткой колеса менять на след. фигуру (зацикленно).
- Каждую новую добавляемую фигуру автоматически добавлять в отдельную группу (<g name="unnamed">) которую потом можно будет переименовать.


Доп. хотелки:
Меню вид -> Мини-карта


(Необязательно) Нужен код в формате Vue инстанца, то есть формат подключения плагинов - цепочкой методов (так легче читается), как в main.ts:
createEditor()
	.addCross() // Центральный крест
	.addGrid() // Сетка

