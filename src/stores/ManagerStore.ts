/*
Глобальный singleton-посредник для межмодульных связей (реализация IoC), а не хранилище сцены.
- manager знает, кому доставить событие;
- не содержит бизнес-логику history/selection/elements;
- не дублирует данные других stores;
- предоставляет типизированные методы;
- модули не обращаются друг к другу напрямую.

Пример вызова: manager.pushHistory(data)
*/